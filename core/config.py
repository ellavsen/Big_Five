STRICT_MODE = True
STRICT_VL_THRESHOLD = 7

# --- Данные пользователя (Этап 3.5) ---
# Психологический профиль — спецкатегория ПДн. Без согласия ничего не собираем.
# Поднимать версию при любом изменении текста в app/consent.py: те, кто согласился
# со старой редакцией, будут спрошены заново.
CONSENT_VERSION = "1"

# Сколько храним разговоры с момента начала сессии. Дальше — удаление каскадом.
RETENTION_DAYS = 180

# --- Mini App (Этап 4C) ---
# Сколько живёт подписанная Telegram initData. Сама подпись бессрочна, поэтому
# без ограничения возраста единожды подсмотренную строку можно было бы
# предъявлять сколько угодно и читать чужой профиль. Час — компромисс:
# сессию в Mini App не рвёт, а украденная строка протухает.
WEBAPP_INIT_DATA_MAX_AGE = 3600

# --- Закрытие черт (Этап 2C, переведено на OCEAN в 3B) ---
# Черта закрывается накоплением веса, а не флагом direct_example у одного сигнала.
# Вес = сумма confidence сигналов одного направления. Ориентиры: модуль даёт 0.25–0.3,
# LLM по умолчанию 0.6. Числа подобраны на сценарном диалоге и рассчитаны на подкрутку
# по живым разговорам — менять здесь, чтобы правка была видна в истории.

# --- Перевод веса наблюдений в оценку черты (Этап 5) ---
# Оценку 0..1 раньше выставляла модель, и она не пользовалась серединой шкалы:
# на живом диалоге ОДНО наблюдение давало 0.70, то есть «черта выражена». В итоге
# все пять черт выходили высокими — профиль приятный и ничего не различающий.
# Теперь оценка считается кодом, а 0.5 честно означает «данных не хватило».

TRAIT_FULL_EVIDENCE = 3.0   # вес, при котором черта считается выраженной в полную силу
TRAIT_MAX_DEVIATION = 0.4   # дальше 0.1..0.9 не уходим: это вероятности, а не диагноз

TRAIT_CLOSE_SCORE = 0.9   # вес ведущего направления (high/low) для жёсткого закрытия
TRAIT_SOFT_SCORE = 0.5    # то же для мягкого; должен быть ≤ TRAIT_CLOSE_SCORE
TRAIT_MIN_SIGNALS = 2     # независимых наблюдений в пользу ведущего направления
TRAIT_DOMINANCE = 1.5     # во сколько раз ведущее направление перевешивает противоположное
