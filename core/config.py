STRICT_MODE = True
STRICT_VL_THRESHOLD = 7

# --- Закрытие осей (Этап 2C) ---
# Ось закрывается накоплением веса, а не флагом direct_example у одного сигнала.
# Вес = сумма confidence сигналов одного направления. Ориентиры: модуль даёт 0.25–0.3,
# LLM по умолчанию 0.6. Числа подобраны на сценарном диалоге и рассчитаны на подкрутку
# по живым разговорам — менять здесь, чтобы правка была видна в истории.

AXIS_CLOSE_SCORE = 0.9   # вес ведущего направления для жёсткого закрытия
AXIS_SOFT_SCORE = 0.5    # то же для мягкого; должен быть ≤ AXIS_CLOSE_SCORE
AXIS_MIN_SIGNALS = 2     # независимых наблюдений в пользу ведущего направления
AXIS_DOMINANCE = 1.5     # во сколько раз ведущее направление перевешивает противоположное
